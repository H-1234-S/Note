
> 使用openspec的开发命令同时加载superpowers工程化有一个问题，ai一直陷入重复读取文件重复读取上下文

因为约束太多，openspec和superpowers都要求先明确需求再开始实现，一旦需求读取失败就会陷入重复读取循环中

> 对于四个变更，如果开四个


>断点：
```
>我要关电脑。请立即停止当前开发动作，只保存断点。

  不要继续实现，不要新增代码，不要提交，不要重建 worktree。
  你最多只能确认 pwd、git branch、git status。
  然后输出：

  1. 当前 worktree 路径
  2. 当前分支
  3. OpenSpec change 名称
  4. 当前做到哪里
  5. 哪些文件已修改
  6. 哪些任务已完成
  7. 下次应该从哪一步继续
  8. 一段下次恢复时可直接粘贴的提示词

  如果某项你不确定，请写“不确定”，不要猜。
  
  请你将断点信息保存到对应工作区内，就放到根目录下就可以，方便下次恢复

```