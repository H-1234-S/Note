# 加入班级

## 题目目标

分别在 PC 端和小程序端实现“查找并加入班级”功能。两端使用不同的布局与交互方式，但必须共用相同的功能流程、数据校验和 Mock API。

完成后的功能应覆盖班级搜索、成员状态展示、身份选择、身份资料填写、表单校验、提交加入和提交后的状态更新。

## 设计图

设计图中的班级名称、创建日期和输入内容仅用于展示页面效果，实际联调数据以 Mock API 返回值为准。

### PC

#### 查找班级

![PC 查找班级](./PC/查找班级.png)

#### 老师身份

![PC 老师身份](./PC/老师身份.png)

#### 学生身份

![PC 学生身份](./PC/学生身份.png)

#### 家长身份

![PC 家长身份](./PC/家长身份.png)

#### 家长多学生

![PC 家长多学生](./PC/家长多学生.png)

### 小程序

#### 按手机号搜索

![小程序按手机号搜索](./小程序/按手机号搜索.png)

#### 按班级口令搜索

![小程序按班级口令搜索](./小程序/按班级口令搜索.png)

#### 老师身份

![小程序老师身份](./小程序/老师身份.png)

#### 学生身份

![小程序学生身份](./小程序/学生身份.png)

#### 家长身份

![小程序家长身份](./小程序/家长身份.png)

#### 家长多学生

![小程序家长多学生](./小程序/家长多学生.png)

## 共用功能

两端均需要实现以下功能：

1. 在“老师手机号”和“班级口令”之间切换搜索方式。
2. 校验搜索内容并请求班级列表。
3. 展示班级名称、群主和当前可执行操作。
4. 未加入的班级展示“加入”，已加入的班级展示“进入班级”。
5. 点击“加入”后选择老师、学生或家长身份。
6. 根据身份动态展示需要填写的资料。
7. 家长身份可以增加或删除学生，支持 1 至 10 名学生。
8. 提交前完成必填、长度、格式和数量校验。
9. 显示请求中状态，避免重复提交。
10. 提交成功后关闭当前表单，并将班级操作更新为“进入班级”。
11. 接口失败时保留用户已填写内容，并显示明确的错误提示。

## 终端差异

### PC

- 使用居中弹窗承载查找和身份表单，不跳转到新的浏览器页面。
- 搜索方式和身份选择使用横向分段按钮。
- 标签与输入控件采用左右对齐的桌面表单布局。
- 家长多学生表单在弹窗内纵向增加，内容较多时允许弹窗内容区滚动。
- 底部同时提供“取消”和“确定”按钮；关闭弹窗前需要妥善处理未提交内容。
- PC 端组件优先使用项目现有的 Ant Design Vue。

### 小程序

- 使用独立页面展示搜索和身份资料，内容按单列纵向排列。
- 搜索方式和身份选择使用适合触控的分段控件。
- 家长多学生通过明显的增加、删除按钮维护。
- 主要提交按钮固定在页面底部，并避开设备安全区。
- 表单、弹层、选择器、按钮和反馈组件优先使用 `libs/vant-ui`。

## 数据与校验规则

### 搜索

- 搜索方式只能是老师手机号或班级口令。
- 搜索内容去除首尾空格后不能为空。
- 手机号必须是以 `1` 开头的 11 位数字。
- 班级口令匹配时不区分英文字母大小写。
- Mock 示例手机号：`13800138000`。
- Mock 示例班级口令：`FRONTEND`。

### 身份

| 身份 | `ClassRole` | 资料要求                                  |
| ---- | ----------: | ----------------------------------------- |
| 老师 |         `1` | `Name` 必填，`Course` 选填                |
| 家长 |         `2` | `Students` 为 1 至 10 项，`Relation` 必填 |
| 学生 |         `3` | `Students` 必须有且只有 1 项              |

### 老师资料

- `Name` 去除首尾空格后为 2 至 20 个字。
- `Course` 允许为空，最多 50 个字。

### 学生资料

- 每项学生资料包含 `Name` 和可选的 `Number`。
- `Name` 去除首尾空格后为 2 至 20 个字。
- `Number` 允许为空；填写时只能包含数字。
- 家长关系值必须是 Mock 支持的 `1`、`2`、`4`、`5`、`6`、`7`、`8`、`9` 或 `99` 之一。

## 共用 Mock API

所有响应都使用项目统一结构：`Type` 表示响应类型，`Data` 保存实际数据，`Description` 保存响应说明。参数错误时 Mock 会抛出带有简体中文原因的错误，页面需要捕获并反馈。

### 搜索班级

`GET /mock/group/search`

#### 查询参数

| 字段      | 类型     | 必填 | 说明                                 |
| --------- | -------- | ---- | ------------------------------------ |
| `Type`    | `0 \| 1` | 是   | `0` 表示老师手机号，`1` 表示班级口令 |
| `Keyword` | `string` | 是   | 手机号或班级口令                     |

#### 请求数据示例

```json
{
  "Type": 0,
  "Keyword": "13800138000"
}
```

#### `Data` 数组项

| 字段             | 类型                   | 说明           |
| -------------- | -------------------- | ------------ |
| `Id`           | `number`             | 班级编号         |
| `Name`         | `string`             | 班级名称         |
| `OwnerName`    | `string`             | 群主名称         |
| `JoinGroupId`  | `string`             | 提交加入时使用的班级标识 |
| `MemberStatus` | `joinable \| joined` | 当前成员状态       |
| `ActionType`   | `join \| enter`      | 页面操作类型       |
| `ActionText`   | `string`             | “加入”或“进入班级”  |

使用示例手机号或口令搜索时，初始返回“高三一班”和“高三二班”。“高三一班”可以加入，“高三二班”已经加入。搜索结果不会返回手机号或班级口令。

### 提交加入资料

`POST /mock/group/join`

#### 共用请求字段

| 字段          | 类型          | 必填 | 说明                 |
| ------------- | ------------- | ---- | -------------------- |
| `GroupId`     | `number`      | 是   | 搜索结果中的班级编号 |
| `GroupName`   | `string`      | 是   | 搜索结果中的班级名称 |
| `JoinGroupId` | `string`      | 是   | 搜索结果中的加入标识 |
| `ClassRole`   | `1 \| 2 \| 3` | 是   | 老师、家长或学生身份 |

#### 身份扩展字段

| 身份 | 字段                                                                     |
| ---- | ------------------------------------------------------------------------ |
| 老师 | `Name: string`、`Course?: string`                                        |
| 学生 | `Students: [{ Name: string, Number?: string }]`                          |
| 家长 | `Students: Array<{ Name: string, Number?: string }>`、`Relation: number` |

#### 家长请求数据示例

```json
{
  "GroupId": 301,
  "GroupName": "高三一班",
  "JoinGroupId": "mock-group-301",
  "ClassRole": 2,
  "Students": [
    {
      "Name": "王小明",
      "Number": "2026001"
    }
  ],
  "Relation": 1
}
```

#### 成功响应的 `Data`

| 字段            | 类型       | 说明              |
| --------------- | ---------- | ----------------- |
| `ApplicationId` | `number`   | Mock 加入申请编号 |
| `MemberId`      | `number`   | Mock 班级成员编号 |
| `GroupId`       | `number`   | 班级编号          |
| `GroupName`     | `string`   | 班级名称          |
| `ClassRole`     | `number`   | 提交的身份值      |
| `Status`        | `joined`   | 加入后的成员状态  |
| `ActionType`    | `enter`    | 加入后的操作类型  |
| `ActionText`    | `进入班级` | 加入后的操作文案  |
| `Message`       | `string`   | 成功提示          |

加入成功后，当前 Mock 进程中的班级会变为已加入状态。再次搜索时应展示“进入班级”；重复提交已加入班级会收到错误。

## 开发约束

- 参与测试人员需要在 `packages/api/src/group` 中自行创建类型、接口声明和模块导出。
- 接口声明必须通过上述 URL 字符串调用项目 `HttpClient`。
- PC 和小程序共用同一套接口类型和声明，不复制两份 API 实现。
- 页面不得直接导入 `packages/api/src/mock/mock-api/join-grade.ts`。
- 页面不得直接调用 `fetch`、Axios、`XMLHttpRequest` 或 `wx.request`。
- 不接入登录、Token、鉴权、真实班级、埋点或外部监控。

## 验收标准

- PC 与小程序均能完成两种搜索方式，并正确处理输入校验。
- 两端均能区分“加入”和“进入班级”状态。
- 老师、学生、家长和家长多学生表单均符合设计图与校验规则。
- 两端调用同一套 `packages/api` 接口声明和 Mock 地址。
- 请求期间有加载或禁用状态，失败和成功均有明确反馈。
- 加入成功后页面状态无需刷新即可更新为“进入班级”。
- 页面不包含登录、真实请求、拖动无关能力或 Mock 实现直连。
