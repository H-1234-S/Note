# Volcano AI 技术设计文档优化清单

## 文档信息

- **优化日期**: 2026-06-15
- **评审人**: Staff Engineer & System Architect
- **目标文档**: TDD_AI文本转PPT微课视频平台.md v1.0.0
- **优化目标**: 修复架构评审发现的严重和重要问题

---

## 🔴 严重问题（必须修复）

### 1. Inngest 事件可靠性保障不完整

**问题位置**: 第 11.4 章一致性设计

**问题描述**: 
当前 Outbox Pattern 实现存在以下漏洞：
1. 缺少事务保护：inngest.send() 成功但 outboxEvent.update() 失败会导致事件重复发送
2. 缺少重试上限：永久失败的事件会无限重试，占用资源
3. 错误处理不完善：仅 console.error 无法触发告警

**优化方案**:

#### 1.1 OutboxEvent 表增加字段

```prisma
model OutboxEvent {
  id            String   @id @default(cuid())
  eventName     String
  payload       Json
  status        String   // Pending / Sent / Failed / DeadLetter
  retryCount    Int      @default(0)
  maxRetries    Int      @default(3)
  lastError     String?
  sentAt        DateTime?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  @@index([status, retryCount])
  @@index([createdAt])
}
```

#### 1.2 改进轮询逻辑

```typescript
// src/server/init/outbox-publisher.ts
import { logger } from '@/lib/logger';
import { alertService } from '@/lib/alert';

setInterval(async () => {
  const events = await db.outboxEvent.findMany({
    where: {
      status: 'Pending',
      retryCount: { lt: db.raw('maxRetries') },
    },
    take: 10,
    orderBy: { createdAt: 'asc' },
  });

  for (const event of events) {
    try {
      // 发送 Inngest 事件
      await inngest.send({
        name: event.eventName,
        data: event.payload,
      });

      // 原子更新（乐观锁）
      await db.outboxEvent.update({
        where: {
          id: event.id,
          status: 'Pending',  // 乐观锁：仅更新仍为 Pending 的记录
        },
        data: {
          status: 'Sent',
          sentAt: new Date(),
        },
      });

      logger.info({
        action: 'outbox.sent',
        eventId: event.id,
        eventName: event.eventName,
        message: 'Outbox event sent successfully',
      });
    } catch (error) {
      const newRetryCount = event.retryCount + 1;
      const newStatus = newRetryCount >= event.maxRetries ? 'DeadLetter' : 'Pending';

      await db.outboxEvent.update({
        where: { id: event.id },
        data: {
          retryCount: newRetryCount,
          status: newStatus,
          lastError: error.message,
          updatedAt: new Date(),
        },
      });

      logger.error({
        action: 'outbox.failed',
        eventId: event.id,
        eventName: event.eventName,
        retryCount: newRetryCount,
        error: error.message,
        message: 'Outbox event failed to send',
      });

      // 进入死信队列时触发告警
      if (newStatus === 'DeadLetter') {
        await alertService.send({
          level: 'critical',
          title: 'Outbox Event Dead Letter',
          message: `Event ${event.id} (${event.eventName}) moved to dead letter queue after ${event.maxRetries} retries`,
          metadata: {
            eventId: event.id,
            eventName: event.eventName,
            lastError: error.message,
          },
        });
      }
    }
  }
}, 5000);  // 每 5 秒轮询一次

logger.info('Outbox publisher started');
```

#### 1.3 死信队列处理

```typescript
// src/server/jobs/dead-letter-handler.ts

/**
 * 死信队列处理器
 * 定期检查死信事件，通知运维人员手动介入
 */
export async function processDeadLetterQueue() {
  const deadLetters = await db.outboxEvent.findMany({
    where: { status: 'DeadLetter' },
    orderBy: { updatedAt: 'desc' },
    take: 50,
  });

  if (deadLetters.length > 0) {
    await alertService.send({
      level: 'warning',
      title: 'Dead Letter Queue Status',
      message: `${deadLetters.length} events in dead letter queue requiring manual review`,
      metadata: {
        events: deadLetters.map(e => ({
          id: e.id,
          eventName: e.eventName,
          lastError: e.lastError,
          createdAt: e.createdAt,
        })),
      },
    });
  }
}

// 每小时检查一次
setInterval(processDeadLetterQueue, 3600_000);
```

---

### 2. 并发控制的 Advisory Lock 实现存在死锁风险

**问题位置**: 第 11.2.2 章幂等实现示例 + EP2-01 Change 文档

**问题描述**:
当前设计使用 `pg_advisory_xact_lock(hashUserId(userId))` 来序列化同一用户的并发请求，但存在以下问题：
1. Hash 冲突风险：hashUserId() 的哈希算法未定义
2. 锁超时未定义：PostgreSQL advisory lock 默认无超时
3. 锁粒度过粗：用户级锁会阻塞该用户的所有操作

**优化方案**:

#### 2.1 hashUserId 实现（使用 CRC32）

```typescript
// src/server/utils/lock.ts
import crc32 from 'crc-32';

/**
 * 将 userId 哈希为 PostgreSQL bigint
 * 使用 CRC32 算法，确保冲突率 < 0.1%
 * 
 * @param userId - 用户 ID（CUID 格式）
 * @returns PostgreSQL bigint（正数空间）
 */
export function hashUserId(userId: string): bigint {
  // CRC32 生成 32 位整数
  const hash = crc32.str(userId);
  
  // 映射到 PostgreSQL bigint 范围的正数空间
  // 偏移 1000000 避免与系统锁冲突（0-999999 保留）
  const lockId = BigInt(Math.abs(hash)) + 1000000n;
  
  return lockId;
}

/**
 * 组合多个键生成锁 ID
 * 用于更细粒度的锁控制
 * 
 * @example
 * // 仅锁住"创建项目"操作，不影响其他操作
 * const lockId = hashKeys(userId, 'create_project');
 */
export function hashKeys(...keys: string[]): bigint {
  const combined = keys.join(':');
  const hash = crc32.str(combined);
  return BigInt(Math.abs(hash)) + 1000000n;
}
```

#### 2.2 锁粒度优化（操作级锁）

```typescript
// src/server/api/routers/project.router.ts

export const createProject = protectedProcedure
  .input(CreateProjectInput)
  .mutation(async ({ ctx, input }) => {
    const { userId } = ctx.session.user;
    
    // 使用操作级锁：仅锁住"创建项目"操作
    // 用户可以同时查询项目列表、查看详情等
    const lockId = hashKeys(userId, 'create_project');
    
    try {
      return await ctx.db.$transaction(async (tx) => {
        // 获取事务级 advisory lock
        // 超时时间由 statement_timeout 控制（需在数据库配置）
        await tx.$executeRawUnsafe(
          `SELECT pg_advisory_xact_lock($1::bigint)`,
          lockId.toString(),
        );
        
        // 幂等检查
        if (input.requestId) {
          const existing = await tx.generationJob.findUnique({
            where: { requestId: input.requestId },
            select: { id: true, projectId: true },
          });
          
          if (existing) {
            throw new TRPCError({
              code: 'CONFLICT',
              message: '重复请求',
              cause: { jobId: existing.id, projectId: existing.projectId },
            });
          }
        }
        
        // 额度检查
        const quota = await checkQuota(userId, ctx.session.user.email);
        if (!quota.canGenerate) {
          throw new TRPCError({
            code: 'TOO_MANY_REQUESTS',
            message: '超出每日生成额度',
            cause: { remaining: quota.remaining },
          });
        }
        
        // 并发限制检查
        const concurrent = await checkConcurrentLimit(userId, ctx.session.user.email);
        if (!concurrent.canStart) {
          throw new TRPCError({
            code: 'TOO_MANY_REQUESTS',
            message: '超出并发生成限制，请等待当前任务完成',
            cause: { runningProjects: concurrent.runningProjects },
          });
        }
        
        // 创建项目和任务
        const project = await tx.project.create({
          data: {
            userId,
            title: input.title,
            sourceText: input.sourceText,
            // ... 其他字段
          },
        });
        
        const job = await tx.generationJob.create({
          data: {
            projectId: project.id,
            userId,
            requestId: input.requestId,
            jobType: 'storyboard',
            status: 'pending',
            inputParams: input,
          },
        });
        
        // Outbox Pattern：写入 OutboxEvent 表
        await tx.outboxEvent.create({
          data: {
            eventName: 'video/generate.requested',
            payload: {
              projectId: project.id,
              userId,
              jobId: job.id,
            },
            status: 'Pending',
          },
        });
        
        return { project, job };
      }, {
        timeout: 10000,  // 事务超时 10 秒
      });
    } catch (error) {
      // 处理唯一约束冲突（极端并发场景的最后防线）
      if (isUniqueConstraintError(error) && getConstraintField(error) === 'requestId') {
        const existing = await ctx.db.generationJob.findUnique({
          where: { requestId: input.requestId },
          select: { id: true, projectId: true },
        });
        
        throw new TRPCError({
          code: 'CONFLICT',
          message: '重复请求（并发冲突）',
          cause: { jobId: existing.id, projectId: existing.projectId },
        });
      }
      
      throw error;
    }
  });
```

#### 2.3 数据库配置（锁超时）

```sql
-- postgresql.conf 或会话级设置
SET statement_timeout = '10s';  -- SQL 语句超时
SET lock_timeout = '5s';        -- 锁等待超时
```

#### 2.4 监控指标

```typescript
// 记录锁等待时间
await recordMetric({
  name: 'advisory_lock_wait_time',
  value: lockWaitMs,
  labels: { operation: 'create_project' },
});

// 告警规则
// lock_wait_time > 3s 触发 P2 告警
```

---

### 3. TTS 音频复用的 checksum 计算逻辑不完整

**问题位置**: 第 6.2.5 章 Asset 表设计

**问题描述**:
文档提到音频复用通过 checksum 字段查询，计算规则为：
`SHA256(textHash + voiceProvider + voiceId + speed)`

但存在以下问题：
1. ❌ **speed 参数未在 Scene 表中定义**
2. ❌ **根据 PRD 和本地知识库，MiniMax TTS API 不支持 speed 参数**
3. textHash 的哈希算法未定义
4. 大小写敏感性未说明
5. 标点符号归一化未定义

**优化方案**:

#### 3.1 修正 checksum 计算规则（移除 speed）

```typescript
// src/server/utils/audio-checksum.ts
import crypto from 'crypto';

/**
 * 计算音频 checksum
 * 用于音频复用：相同文本+语音生成的音频可复用
 * 
 * 规则：SHA256(normalizedText + voiceProvider + voiceId)
 * 
 * 注意：不包含 speed 参数，因为 MiniMax TTS API 不支持调速
 */
export function calculateAudioChecksum(params: {
  text: string;
  voiceProvider: string;
  voiceId: string;
}): string {
  // 1. 文本归一化
  const normalizedText = normalizeText(params.text);
  
  // 2. 组合参数（使用 | 分隔符避免碰撞）
  const combined = `${normalizedText}|${params.voiceProvider}|${params.voiceId}`;
  
  // 3. SHA256 计算
  const checksum = crypto
    .createHash('sha256')
    .update(combined, 'utf8')
    .digest('hex');
  
  return checksum;
}

/**
 * 文本归一化
 * 确保语义相同的文本生成相同的 checksum
 */
function normalizeText(text: string): string {
  return text
    .trim()                          // 去除首尾空格
    .replace(/\s+/g, ' ')            // 多个空格合并为一个
    .replace(/[!！]/g, '!')          // 中英文标点归一化
    .replace(/[?？]/g, '?')
    .replace(/[,，]/g, ',')
    .replace(/[。.]/g, '.')
    .replace(/[;；]/g, ';')
    .replace(/[:|：]/g, ':')
    .toLowerCase();                  // 转小写（中文不受影响）
}

/**
 * 示例
 */
const checksum1 = calculateAudioChecksum({
  text: '你好！世界。',
  voiceProvider: 'minimax',
  voiceId: 'female-yoyo',
});

const checksum2 = calculateAudioChecksum({
  text: '你好!  世界.',  // 不同标点、多余空格
  voiceProvider: 'minimax',
  voiceId: 'female-yoyo',
});

// checksum1 === checksum2 ✅
```

#### 3.2 Scene 表不增加 speed 字段

Scene 表保持当前设计，不增加 speed 字段：

```prisma
model Scene {
  id                    String   @id @default(cuid())
  projectId             String
  storyboardVersionId   String
  order                 Int
  sceneType             SceneType?
  title                 String?
  narrationText         Text
  visualDescription     Text?
  visualJson            Json?
  animationPreset       String?
  emotionalTone         String?
  audioAssetId          String?
  durationSec           Float?
  startTimeSec          Float?
  captionsJson          Json?
  imageAssetId          String?
  createdAt             DateTime @default(now())
  
  // ❌ 不增加 speed 字段（MiniMax 不支持）
  
  @@unique([storyboardVersionId, order])
  @@index([storyboardVersionId, order])
}
```

#### 3.3 Asset 表 metadata 字段记录 TTS 参数

```prisma
model Asset {
  // ... 其他字段
  
  metadata    Json?    // 扩展字段（如 TTS providerRequestId）
  
  // metadata 结构示例（音频类型）：
  // {
  //   "voiceProvider": "minimax",
  //   "voiceId": "female-yoyo",
  //   "providerRequestId": "req_abc123",
  //   "textLength": 150,
  //   "normalizedText": "你好!世界."  // 可选：用于调试
  // }
}
```

---
