# Volcano AI 技术设计文档优化清单

## 文档信息

- **优化日期**: 2026-06-15
- **评审人**: Staff Engineer & System Architect
- **目标文档**: TDD_AI文本转PPT微课视频平台.md v1.0.0
- **优化目标**: 修复架构评审发现的严重和重要问题

---

## 🔴 严重问题（必须修复）

### 1. Inngest 事件可靠性保障不完整

**问题位置**: 第 11.4 章一致性设计

**问题描述**: 
当前 Outbox Pattern 实现存在以下漏洞：
1. 缺少事务保护：inngest.send() 成功但 outboxEvent.update() 失败会导致事件重复发送
2. 缺少重试上限：永久失败的事件会无限重试，占用资源
3. 错误处理不完善：仅 console.error 无法触发告警

**优化方案**:

#### 1.1 OutboxEvent 表增加字段

```prisma
model OutboxEvent {
  id            String   @id @default(cuid())
  eventName     String
  payload       Json
  status        String   // Pending / Sent / Failed / DeadLetter
  retryCount    Int      @default(0)
  maxRetries    Int      @default(3)
  lastError     String?
  sentAt        DateTime?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  @@index([status, retryCount])
  @@index([createdAt])
}
```

#### 1.2 改进轮询逻辑

```typescript
// src/server/init/outbox-publisher.ts
import { logger } from '@/lib/logger';
import { alertService } from '@/lib/alert';

setInterval(async () => {
  const events = await db.outboxEvent.findMany({
    where: {
      status: 'Pending',
      retryCount: { lt: db.raw('maxRetries') },
    },
    take: 10,
    orderBy: { createdAt: 'asc' },
  });

  for (const event of events) {
    try {
      // 发送 Inngest 事件
      await inngest.send({
        name: event.eventName,
        data: event.payload,
      });

      // 原子更新（乐观锁）
      await db.outboxEvent.update({
        where: {
          id: event.id,
          status: 'Pending',  // 乐观锁：仅更新仍为 Pending 的记录
        },
        data: {
          status: 'Sent',
          sentAt: new Date(),
        },
      });

      logger.info({
        action: 'outbox.sent',
        eventId: event.id,
        eventName: event.eventName,
        message: 'Outbox event sent successfully',
      });
    } catch (error) {
      const newRetryCount = event.retryCount + 1;
      const newStatus = newRetryCount >= event.maxRetries ? 'DeadLetter' : 'Pending';

      await db.outboxEvent.update({
        where: { id: event.id },
        data: {
          retryCount: newRetryCount,
          status: newStatus,
          lastError: error.message,
          updatedAt: new Date(),
        },
      });

      logger.error({
        action: 'outbox.failed',
        eventId: event.id,
        eventName: event.eventName,
        retryCount: newRetryCount,
        error: error.message,
        message: 'Outbox event failed to send',
      });

      // 进入死信队列时触发告警
      if (newStatus === 'DeadLetter') {
        await alertService.send({
          level: 'critical',
          title: 'Outbox Event Dead Letter',
          message: `Event ${event.id} (${event.eventName}) moved to dead letter queue after ${event.maxRetries} retries`,
          metadata: {
            eventId: event.id,
            eventName: event.eventName,
            lastError: error.message,
          },
        });
      }
    }
  }
}, 5000);  // 每 5 秒轮询一次

logger.info('Outbox publisher started');
```

#### 1.3 死信队列处理

```typescript
// src/server/jobs/dead-letter-handler.ts

/**
 * 死信队列处理器
 * 定期检查死信事件，通知运维人员手动介入
 */
export async function processDeadLetterQueue() {
  const deadLetters = await db.outboxEvent.findMany({
    where: { status: 'DeadLetter' },
    orderBy: { updatedAt: 'desc' },
    take: 50,
  });

  if (deadLetters.length > 0) {
    await alertService.send({
      level: 'warning',
      title: 'Dead Letter Queue Status',
      message: `${deadLetters.length} events in dead letter queue requiring manual review`,
      metadata: {
        events: deadLetters.map(e => ({
          id: e.id,
          eventName: e.eventName,
          lastError: e.lastError,
          createdAt: e.createdAt,
        })),
      },
    });
  }
}

// 每小时检查一次
setInterval(processDeadLetterQueue, 3600_000);
```

---

### 2. 并发控制的 Advisory Lock 实现存在死锁风险

**问题位置**: 第 11.2.2 章幂等实现示例 + EP2-01 Change 文档

**问题描述**:
当前设计使用 `pg_advisory_xact_lock(hashUserId(userId))` 来序列化同一用户的并发请求，但存在以下问题：
1. Hash 冲突风险：hashUserId() 的哈希算法未定义
2. 锁超时未定义：PostgreSQL advisory lock 默认无超时
3. 锁粒度过粗：用户级锁会阻塞该用户的所有操作

**优化方案**:

#### 2.1 hashUserId 实现（使用 CRC32）

```typescript
// src/server/utils/lock.ts
import crc32 from 'crc-32';

/**
 * 将 userId 哈希为 PostgreSQL bigint
 * 使用 CRC32 算法，确保冲突率 < 0.1%
 * 
 * @param userId - 用户 ID（CUID 格式）
 * @returns PostgreSQL bigint（正数空间）
 */
export function hashUserId(userId: string): bigint {
  // CRC32 生成 32 位整数
  const hash = crc32.str(userId);
  
  // 映射到 PostgreSQL bigint 范围的正数空间
  // 偏移 1000000 避免与系统锁冲突（0-999999 保留）
  const lockId = BigInt(Math.abs(hash)) + 1000000n;
  
  return lockId;
}

/**
 * 组合多个键生成锁 ID
 * 用于更细粒度的锁控制
 * 
 * @example
 * // 仅锁住"创建项目"操作，不影响其他操作
 * const lockId = hashKeys(userId, 'create_project');
 */
export function hashKeys(...keys: string[]): bigint {
  const combined = keys.join(':');
  const hash = crc32.str(combined);
  return BigInt(Math.abs(hash)) + 1000000n;
}
```

#### 2.2 锁粒度优化（操作级锁）

```typescript
// src/server/api/routers/project.router.ts

export const createProject = protectedProcedure
  .input(CreateProjectInput)
  .mutation(async ({ ctx, input }) => {
    const { userId } = ctx.session.user;
    
    // 使用操作级锁：仅锁住"创建项目"操作
    // 用户可以同时查询项目列表、查看详情等
    const lockId = hashKeys(userId, 'create_project');
    
    try {
      return await ctx.db.$transaction(async (tx) => {
        // 获取事务级 advisory lock
        // 超时时间由 statement_timeout 控制（需在数据库配置）
        await tx.$executeRawUnsafe(
          `SELECT pg_advisory_xact_lock($1::bigint)`,
          lockId.toString(),
        );
        
        // 幂等检查
        if (input.requestId) {
          const existing = await tx.generationJob.findUnique({
            where: { requestId: input.requestId },
            select: { id: true, projectId: true },
          });
          
          if (existing) {
            throw new TRPCError({
              code: 'CONFLICT',
              message: '重复请求',
              cause: { jobId: existing.id, projectId: existing.projectId },
            });
          }
        }
        
        // 额度检查
        const quota = await checkQuota(userId, ctx.session.user.email);
        if (!quota.canGenerate) {
          throw new TRPCError({
            code: 'TOO_MANY_REQUESTS',
            message: '超出每日生成额度',
            cause: { remaining: quota.remaining },
          });
        }
        
        // 并发限制检查
        const concurrent = await checkConcurrentLimit(userId, ctx.session.user.email);
        if (!concurrent.canStart) {
          throw new TRPCError({
            code: 'TOO_MANY_REQUESTS',
            message: '超出并发生成限制，请等待当前任务完成',
            cause: { runningProjects: concurrent.runningProjects },
          });
        }
        
        // 创建项目和任务
        const project = await tx.project.create({
          data: {
            userId,
            title: input.title,
            sourceText: input.sourceText,
            // ... 其他字段
          },
        });
        
        const job = await tx.generationJob.create({
          data: {
            projectId: project.id,
            userId,
            requestId: input.requestId,
            jobType: 'storyboard',
            status: 'pending',
            inputParams: input,
          },
        });
        
        // Outbox Pattern：写入 OutboxEvent 表
        await tx.outboxEvent.create({
          data: {
            eventName: 'video/generate.requested',
            payload: {
              projectId: project.id,
              userId,
              jobId: job.id,
            },
            status: 'Pending',
          },
        });
        
        return { project, job };
      }, {
        timeout: 10000,  // 事务超时 10 秒
      });
    } catch (error) {
      // 处理唯一约束冲突（极端并发场景的最后防线）
      if (isUniqueConstraintError(error) && getConstraintField(error) === 'requestId') {
        const existing = await ctx.db.generationJob.findUnique({
          where: { requestId: input.requestId },
          select: { id: true, projectId: true },
        });
        
        throw new TRPCError({
          code: 'CONFLICT',
          message: '重复请求（并发冲突）',
          cause: { jobId: existing.id, projectId: existing.projectId },
        });
      }
      
      throw error;
    }
  });
```

#### 2.3 数据库配置（锁超时）

```sql
-- postgresql.conf 或会话级设置
SET statement_timeout = '10s';  -- SQL 语句超时
SET lock_timeout = '5s';        -- 锁等待超时
```

#### 2.4 监控指标

```typescript
// 记录锁等待时间
await recordMetric({
  name: 'advisory_lock_wait_time',
  value: lockWaitMs,
  labels: { operation: 'create_project' },
});

// 告警规则
// lock_wait_time > 3s 触发 P2 告警
```

---

### 3. TTS 音频复用的 checksum 计算逻辑不完整

**问题位置**: 第 6.2.5 章 Asset 表设计

**问题描述**:
文档提到音频复用通过 checksum 字段查询，计算规则为：
`SHA256(textHash + voiceProvider + voiceId + speed)`

但存在以下问题：
1. ❌ **speed 参数未在 Scene 表中定义**
2. ❌ **根据 PRD 和本地知识库，MiniMax TTS API 不支持 speed 参数**
3. textHash 的哈希算法未定义
4. 大小写敏感性未说明
5. 标点符号归一化未定义

**优化方案**:

#### 3.1 修正 checksum 计算规则（移除 speed）

```typescript
// src/server/utils/audio-checksum.ts
import crypto from 'crypto';

/**
 * 计算音频 checksum
 * 用于音频复用：相同文本+语音生成的音频可复用
 * 
 * 规则：SHA256(normalizedText + voiceProvider + voiceId)
 * 
 * 注意：不包含 speed 参数，因为 MiniMax TTS API 不支持调速
 */
export function calculateAudioChecksum(params: {
  text: string;
  voiceProvider: string;
  voiceId: string;
}): string {
  // 1. 文本归一化
  const normalizedText = normalizeText(params.text);
  
  // 2. 组合参数（使用 | 分隔符避免碰撞）
  const combined = `${normalizedText}|${params.voiceProvider}|${params.voiceId}`;
  
  // 3. SHA256 计算
  const checksum = crypto
    .createHash('sha256')
    .update(combined, 'utf8')
    .digest('hex');
  
  return checksum;
}

/**
 * 文本归一化
 * 确保语义相同的文本生成相同的 checksum
 */
function normalizeText(text: string): string {
  return text
    .trim()                          // 去除首尾空格
    .replace(/\s+/g, ' ')            // 多个空格合并为一个
    .replace(/[!！]/g, '!')          // 中英文标点归一化
    .replace(/[?？]/g, '?')
    .replace(/[,，]/g, ',')
    .replace(/[。.]/g, '.')
    .replace(/[;；]/g, ';')
    .replace(/[:|：]/g, ':')
    .toLowerCase();                  // 转小写（中文不受影响）
}

/**
 * 示例
 */
const checksum1 = calculateAudioChecksum({
  text: '你好！世界。',
  voiceProvider: 'minimax',
  voiceId: 'female-yoyo',
});

const checksum2 = calculateAudioChecksum({
  text: '你好!  世界.',  // 不同标点、多余空格
  voiceProvider: 'minimax',
  voiceId: 'female-yoyo',
});

// checksum1 === checksum2 ✅
```

#### 3.2 Scene 表不增加 speed 字段

Scene 表保持当前设计，不增加 speed 字段：

```prisma
model Scene {
  id                    String   @id @default(cuid())
  projectId             String
  storyboardVersionId   String
  order                 Int
  sceneType             SceneType?
  title                 String?
  narrationText         Text
  visualDescription     Text?
  visualJson            Json?
  animationPreset       String?
  emotionalTone         String?
  audioAssetId          String?
  durationSec           Float?
  startTimeSec          Float?
  captionsJson          Json?
  imageAssetId          String?
  createdAt             DateTime @default(now())
  
  // ❌ 不增加 speed 字段（MiniMax 不支持）
  
  @@unique([storyboardVersionId, order])
  @@index([storyboardVersionId, order])
}
```

#### 3.3 Asset 表 metadata 字段记录 TTS 参数

```prisma
model Asset {
  // ... 其他字段
  
  metadata    Json?    // 扩展字段（如 TTS providerRequestId）
  
  // metadata 结构示例（音频类型）：
  // {
  //   "voiceProvider": "minimax",
  //   "voiceId": "female-yoyo",
  //   "providerRequestId": "req_abc123",
  //   "textLength": 150,
  //   "normalizedText": "你好!世界."  // 可选：用于调试
  // }
}
```

---

## 🟡 重要问题（强烈建议修复）

### 4. Remotion Worker 架构缺少故障隔离设计

**问题位置**: 第 3 章总体架构设计 + 第 14 章部署架构

**详细优化方案见**: 补充文档第 14.3 章节

**要点**:
1. 第一版：单实例 Worker + 内存队列（最大 10 个任务）
2. 故障处理：Docker 自动重启 + Inngest 重试
3. 未来扩展：多实例 Worker + Redis Bull Queue

---

### 5. 数据库索引策略补充查询模式分析

**问题位置**: 第 6.2 章核心表设计

**优化内容**:

#### 5.1 Project 表索引分析

| 索引 | 查询场景 | 是否必需 | 说明 |
|------|---------|---------|------|
| `userId_createdAt_idx` | 用户项目列表 | ✅ 必需 | 覆盖 95% 查询 |
| `status_idx` | 管理员按状态筛选 | ⚠️ 可选 | 【待确认】管理员查询频率 |

#### 5.2 GenerationJob 表新增索引（Stalled Job 检测）

```sql
CREATE INDEX idx_generationjob_stalled_detection 
ON GenerationJob(status, updatedAt)
WHERE status IN ('pending', 'running', 'retrying');
```

---

### 6. API 限流策略补充实现细节

**问题位置**: 第 7.2 章 API 设计

**优化内容**:

| API | 限流键 | 限流值 | 窗口 | 超限响应 |
|-----|--------|--------|------|---------|
| `project.createAndGenerate` | `userId` | 10 次 | 1 分钟 | `retryAfter: 60s` |
| `project.list` | `userId` | 50 次 | 1 分钟 | `RATE_LIMIT_EXCEEDED` |
| 所有 API（防 DDoS） | `ip` | 100 次 | 1 分钟 | 429 |

**前端防抖**: 
```typescript
const handleCreate = debounce(() => {
  createMutation.mutate(formData);
}, 1000, { leading: true, trailing: false });
```

---

### 7. 错误码体系统一标准

**问题位置**: 第 7.2 章 API 设计

**错误码格式**: `<DOMAIN>_<ERROR_TYPE>_<DETAIL>`

**核心错误码清单**:

| 错误码 | HTTP | 说明 | 可重试 |
|--------|------|------|--------|
| `PROJECT_QUOTA_EXCEEDED` | 429 | 超出额度 | ❌ |
| `PROJECT_CONCURRENT_LIMIT_EXCEEDED` | 429 | 并发超限 | ✅ (60s后) |
| `PROVIDER_LLM_UNAVAILABLE` | 503 | LLM不可用 | ✅ (指数退避) |
| `PROVIDER_TTS_RATE_LIMIT` | 429 | TTS限流 | ✅ (按API返回) |
| `RENDER_TIMEOUT` | 504 | 渲染超时 | ✅ (Inngest重试) |

---

## 📋 架构决策记录（ADR）

### ADR-001: Inngest 事件可靠性保障方案

**状态**: 已决策  
**决策日期**: 2026-06-15  
**决策人**: Tech Lead

**上下文**:
Inngest 事件发送可能因网络异常失败，导致项目卡在 queued 状态。

**备选方案**:
- 方案 A: Outbox Pattern + 定时重试 + 死信队列
- 方案 B: 监控 + 人工介入
- 方案 C: 使用 Kafka 替代 Inngest

**决策**: 采用方案 A

**理由**:
- 保证最终一致性
- 实现成本低
- 无需人工介入
- 死信队列兜底机制

**实现要点**:
1. OutboxEvent 表增加 `retryCount`, `maxRetries`, `lastError` 字段
2. 轮询器使用乐观锁更新状态
3. 重试次数达到上限后进入死信队列
4. 死信队列触发 P1 告警

---

### ADR-002: 音频复用 checksum 计算规则

**状态**: 已决策  
**决策日期**: 2026-06-15  
**决策人**: Tech Lead

**上下文**:
相同文本和语音的音频应该复用，减少 TTS 成本和生成时间。

**决策**: checksum = `SHA256(normalizedText + voiceProvider + voiceId)`

**理由**:
- ❌ **不包含 speed 参数**：根据 PRD 和 MiniMax API 文档，MiniMax TTS 不支持 speed 参数
- 文本归一化：去除空格差异、标点差异、大小写差异
- SHA256 哈希：碰撞概率极低（< 2^-128）

**文本归一化规则**:
```typescript
function normalizeText(text: string): string {
  return text
    .trim()
    .replace(/\s+/g, ' ')      // 多空格→单空格
    .replace(/[!！]/g, '!')    // 中英文标点归一化
    .replace(/[?？]/g, '?')
    .replace(/[,，]/g, ',')
    .replace(/[。.]/g, '.')
    .toLowerCase();            // 转小写
}
```

---

### ADR-003: Advisory Lock 实现算法

**状态**: 已决策  
**决策日期**: 2026-06-15  
**决策人**: Tech Lead

**上下文**:
需要序列化同一用户的并发"创建项目"请求，避免额度检查和并发检查的竞态条件。

**决策**: 使用 PostgreSQL advisory lock + CRC32 哈希

**实现**:
```typescript
export function hashUserId(userId: string): bigint {
  const hash = crc32.str(userId);
  return BigInt(Math.abs(hash)) + 1000000n;  // 偏移避免系统锁冲突
}
```

**锁粒度优化**: 
- ❌ 不使用用户级锁（会阻塞用户所有操作）
- ✅ 使用操作级锁：`hashKeys(userId, 'create_project')`

**超时配置**:
- `statement_timeout = 10s`：SQL 语句超时
- `lock_timeout = 5s`：锁等待超时

**监控**:
- 记录 `advisory_lock_wait_time` 指标
- lock_wait_time > 3s 触发 P2 告警

---

### ADR-004: Remotion Worker 部署方案

**状态**: 已决策  
**决策日期**: 2026-06-15  
**决策人**: Tech Lead

**上下文**:
需要独立部署 Remotion Worker 避免阻塞 Next.js Web 服务，同时控制成本。

**备选方案**:
- 方案 A: 单实例 Worker + 内存队列
- 方案 B: 多实例 Worker + Redis Bull Queue
- 方案 C: Remotion Lambda（Serverless）

**决策**: 第一版采用方案 A，预留方案 B 迁移路径

**理由**:
- 方案 A 成本低（单台 EC2），满足第一版并发需求
- 方案 B 适合高并发场景（> 5 视频/分钟）
- 方案 C 成本不可控，不适合 MVP

**方案 A 限制**:
- 同时渲染 1 个视频（避免 OOM）
- 队列最大 10 个任务
- 队列满时返回 503，Inngest 延迟重试

**故障恢复**:
- Docker 自动重启（--restart=always）
- Inngest 超时重试（渲染超时 > 15 分钟）

---

## 📊 待确认清单

| # | 问题 | 优先级 | 建议 | 责任人 |
|---|------|--------|------|--------|
| 1 | 管理员查询项目列表的频率 | 中 | 如果 < 1% 可移除 status_idx | Backend |
| 2 | DeepSeek API 平均响应时间 | 高 | 压测验证，确认性能目标可达 | Backend |
| 3 | MiniMax TTS 批量并发调用性能 | 高 | 压测验证，确认 P95 < 40s | Backend |
| 4 | Remotion 渲染实际耗时 | 高 | 在目标硬件（8C 32G）压测 | DevOps + Backend |
| 5 | 每日免费生成次数 | 高 | MVP 默认 1 次还是 3 次？ | PM |

---

## ✅ 实施建议

### 优先级 P0（必须在第一版实现）

1. ✅ 修复 Inngest 事件可靠性（增加死信队列）
2. ✅ 明确 advisory lock 实现（CRC32 哈希）
3. ✅ 修正音频复用 checksum（移除 speed）
4. ✅ 补充 Remotion Worker 部署方案

### 优先级 P1（强烈建议实现）

5. ⚠️ 补充数据库索引分析和监控
6. ⚠️ 完善 API 限流实现细节
7. ⚠️ 统一错误码体系

### 优先级 P2（可延后到第二版）

8. 📌 Remotion Worker 多实例扩展
9. 📌 Outbox 轮询改为 LISTEN/NOTIFY
10. 📌 数据库读写分离

---

## 🔧 快速修改指南

### 修改 1：OutboxEvent 表 Schema

**文件**: `prisma/schema.prisma`

```diff
model OutboxEvent {
  id            String   @id @default(cuid())
  eventName     String
  payload       Json
- status        String   // Pending / Sent
+ status        String   // Pending / Sent / Failed / DeadLetter
+ retryCount    Int      @default(0)
+ maxRetries    Int      @default(3)
+ lastError     String?
  sentAt        DateTime?
  createdAt     DateTime @default(now())
+ updatedAt     DateTime @updatedAt
  
+ @@index([status, retryCount])
  @@index([createdAt])
}
```

### 修改 2：音频 checksum 计算

**文件**: `src/server/utils/audio-checksum.ts`

```typescript
// ❌ 错误（包含 speed）
const checksum = SHA256(textHash + voiceProvider + voiceId + speed);

// ✅ 正确（移除 speed）
export function calculateAudioChecksum(params: {
  text: string;
  voiceProvider: string;
  voiceId: string;
}): string {
  const normalizedText = normalizeText(params.text);
  const combined = `${normalizedText}|${params.voiceProvider}|${params.voiceId}`;
  return crypto.createHash('sha256').update(combined, 'utf8').digest('hex');
}
```

### 修改 3：advisory lock 哈希函数

**文件**: `src/server/utils/lock.ts`

```typescript
import crc32 from 'crc-32';

export function hashUserId(userId: string): bigint {
  const hash = crc32.str(userId);
  return BigInt(Math.abs(hash)) + 1000000n;
}

export function hashKeys(...keys: string[]): bigint {
  const combined = keys.join(':');
  const hash = crc32.str(combined);
  return BigInt(Math.abs(hash)) + 1000000n;
}
```

---

## 📚 参考资料

- [PostgreSQL Advisory Locks](https://www.postgresql.org/docs/current/explicit-locking.html#ADVISORY-LOCKS)
- [Rate Limiter Flexible](https://github.com/animir/node-rate-limiter-flexible)
- [Remotion Deployment Guide](https://www.remotion.dev/docs/lambda)
- [Prisma Indexing Best Practices](https://www.prisma.io/docs/guides/performance-and-optimization/query-optimization-performance)
- [MiniMax TTS API Documentation](https://platform.minimaxi.com/document/T2A%20V2)

---

**文档结束**
