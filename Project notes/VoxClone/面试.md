
你提到"全链路类型安全"，从数据库到前端组件的完整类型链路。请描述一下这个类型链路具体是怎么实现的？Prisma 生成的类型是如何流转到前端 tRPC 调用的？

首先，在Schema.prisma中，定义了voices和generation两张表，对于这两张表的所有字段和该字段的类型，在服务端调用prisma实例操作数据库时，前端都有完整的类型提示，同时如果更改字段，前端也会有类型提示和代码补全，对于返回值同样也有完整的类型提示，没看懂Prisma 生成的类型是如何流转到前端 tRPC 调用的？这个问题

---

tRPC 和 TanStack Query都在做类型安全，它们各自的职责是什么？你为什么需要两层类型检查，是否有冗余？

