# 并发渲染

将同步更新变为可中断的异步更新，其实就是react在主线程上把渲染任务拆成一个个小的工作单元并且协作式的让出控制权，让浏览器可以优先响应高优先级的任务，对于低优先级可中断

问题：

怎么判断该不该中断？
	react有一个work loop函数，函数中判断该不该中断，如果该中断则退出执行
怎么生成最小工作单元？
	work loop函数中的performUnitOfWork函数生成最小工作单元，同时还渲染dom、处理子节点
可中断那么怎么恢复？
	引入了一种架构也可以叫数据结构fiber，让react有了可以中断、暂停、恢复的能力

