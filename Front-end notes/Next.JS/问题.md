# 边界问题

## RSC组件

Server Component 运行在服务器环境

## RCC组件

Client Component 运行在浏览器环境

## 边界

RSC组件可以作为RCC组件的子组件，**原因：** 服务端组件会先在服务器渲染好，然后作为 **静态结果** 传给客户端组件

```
ServerComponent → 先变成 HTML → ClientComponent 再接收
```
