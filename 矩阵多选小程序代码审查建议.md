# 矩阵多选小程序端代码审查报告

- 审查范围：`apps/miniprogram/src/field/pages/create-matrix-checkbox/`（`index.ts` / `index.wxml` / `index.scss` / `index.json`）
- 关联类型：`apps/miniprogram/src/types/field/index.ts`
- 需求依据：`题目/003.矩阵多选/功能介绍.md`
- 审查视角：资深前端 / 资深小程序开发工程师

---

## 一、总体结论

功能主链路（拉取模板 → 编辑行列 → 设置数量/开关 → 预览 → 保存）已经打通，整体结构清晰，`setData` 的不可变更新写法（展开 + 重建引用）也符合小程序最佳实践，事件取值方式（`event.detail` 即字符串）经核对 Vant `field` 源码是正确的。

但存在 **3 个会直接违反验收标准的功能缺失/缺陷** 和 **若干逻辑不一致**，需要修复后才能算完整达标：

| 级别 | 问题 | 位置 |
| --- | --- | --- |
| 🔴 高 | 保存成功后没有任何成功反馈，只剩 `console.log` + `TODO` | `index.ts` 338-339 |
| 🔴 高 | 批量编辑的空行过滤只处理了空字符串，未过滤「纯空格行」 | `index.ts` 241-247 |
| 🔴 高 | 批量编辑后未调用 `clampSelectCount`，选项减少时选择数量会失效 | `index.ts` 237-261 |
| 🟠 中 | 添加行标题/选项与保存校验都缺少「最多 20 / 10 项」上限 | `index.ts` 119、133、363-369 |
| 🟠 中 | 数量选择器打开时未回显当前值，永远默认选中「无限」 | `index.ts` 282-318 |
| 🟠 中 | 列表渲染用 `wx:key="index"`，未使用需求强调的稳定编号 `Value` | `index.wxml` 多处 |
| 🟡 低 | 页面用 `Component()` 而非项目统一的 `Page()`；空 `lifetimes.attached` | `index.ts` 10、62-66 |
| 🟡 低 | `onSubmit` 缺少防重入判断；加载模板无 loading 状态 | `index.ts` 320、82-96 |
| 🟡 低 | 保存成功文本未在模板回填后同步；`console.log` 遗留 | `index.ts` 90-92 |
| 🟡 低 | 页面样式 `.van-field-body` 全局穿透不生效；footer 有死占位 | `index.scss` 118-120 |

---

## 二、逐条详细分析与修改建议

### 🔴 1. 保存成功后没有成功反馈

**现状**（`index.ts:338-339`）：

```ts
// TODO:展示数据？
console.log(response)
```

**问题**：需求第 10 条「显示模板加载、保存中、保存成功和保存失败状态」是硬性验收项。目前保存失败（`response.Error` + `catch`）有提示，但成功路径只打印日志。`console.log` 属于遗留调试代码（文件头还特意 `eslint-disable no-console` 掩盖）。

**建议**：

```ts
if (response.Error) {
  wx.showToast({ title: response.Error, icon: 'none' })
  return
}
// 成功：明确反馈 + 使用规范化后的字段
wx.showToast({ title: response.Data?.Message || '保存成功', icon: 'success' })
// 如需回显保存后的规范化字段（需求第 199 行提到 Data.Field）
this.setData({ formData: response.Data!.Field })
```

同时删除文件头 `/* eslint-disable no-console */` 和 `console.log`。

---

### 🔴 2. 批量编辑「空行」过滤不完整

**现状**（`index.ts:241-247`）：

```ts
const result = textAreaText
  .split('\n')
  .filter(item => item !== '')        // 只过滤空字符串
  .map(item => ({
    Text: item.trim(),
    Value: _actionType === 'title' ? ++TitleMaxIndex : ++OptionMaxIndex
  }))
```

**问题**：需求第 5 条要求「过滤空行并去除首尾空格」。若某一行是 `"   "`（若干空格），`item !== ''` 为 `true`，该行被保留，`trim()` 后 `Text` 变成空字符串，提交时被 Mock 校验拦截（「内容不能为空」）。前端应主动过滤。

**建议**：

```ts
const result = textAreaText
  .split('\n')
  .filter(item => item.trim() !== '') // 先 trim 再判空，覆盖纯空格行
  .map(item => ({
    Text: item.trim(),
    Value: _actionType === 'title' ? ++TitleMaxIndex : ++OptionMaxIndex
  }))
```

---

### 🔴 3. 批量编辑后未修正选择数量

**现状**（`index.ts:237-261`）：`onConfirmDialog` 直接整体替换 `Titles` / `Options`，未调用 `clampSelectCount`。

**问题**：批量编辑选项把数量从 5 行改成 2 行后，`MinSelectCount` / `MaxSelectCount` 可能仍为 3、4，此时预览与设置自相矛盾。虽然保存时 `verification` 会拦截（「最少应选不得大于当前选项数量」），但需求第 114 行明确「页面需要主动将失效数量调整到合法范围，**或**阻止保存并给出提示」。删除单条选项时你已经做了主动 `clampSelectCount`（`index.ts:163`），批量编辑却漏了，行为不一致。

**建议**：在 `onConfirmDialog` 的 `setData` 前，若 `_actionType === 'option'` 则补一次修正；最干净的做法是在 `setData` 完成后调用 `this.clampSelectCount()`（它内部读 `this.data` 最新值），并让 `clampSelectCount` 也处理「Min > Max」的交叉修正（见下）：

```ts
this.setData({
  showBatchEditDialog: false,
  formData: { ... }
})

// 选项数量可能变化，修正选择范围
if (_actionType === 'option') {
  this.clampSelectCount()
}
```

> 补充建议：当前 `clampSelectCount` 只修正了「大于选项数」，未处理「`MaxSelectCount` 非零时 `MinSelectCount > MaxSelectCount`」。建议一并修正：
>
> ```ts
> if (MaxSelectCount !== 0 && MinSelectCount > MaxSelectCount) {
>   matrixPatch.MinSelectCount = MaxSelectCount
>   displayPatch.minimumSelectableText = String(MaxSelectCount)
> }
> ```

---

### 🟠 4. 缺少「最多 20 / 10 项」上限校验

**现状**：`addTitleAction`（119）与 `addOptionAction`（133）可无限追加；`verification`（363-369）只校验了下限 `Titles.length < 2`、`Options.length < 2`，没有上限。

**问题**：需求明确「行标题最多 20 项」「选项最多 10 项」。现在用户能加到任意多，直到提交时 Mock 才报「最多只能设置 20 项」，体验差且不符合验收。

**建议**：在添加入口拦截（即时反馈），并在 `verification` 兜底：

```ts
addTitleAction() {
  const { Titles, TitleMaxIndex } = this.data.formData.Matrix
  if (Titles.length >= 20) {
    wx.showToast({ title: '行标题最多 20 项', icon: 'none' })
    return
  }
  // ...
}

addOptionAction() {
  const { Options, OptionMaxIndex } = this.data.formData.Matrix
  if (Options.length >= 10) {
    wx.showToast({ title: '选项最多 10 项', icon: 'none' })
    return
  }
  // ...
}
```

`verification` 中补充：

```ts
if (Titles.length > 20) return '行标题最多 20 项'
if (Options.length > 10) return '选项最多 10 项'
```

---

### 🟠 5. 数量选择器打开时未回显当前值

**现状**（`index.ts:282-288`）：`onShowOptionalNumberPopup` 只 `showNumberPopup: true`，`van-picker` 没有 `default-index`，每次打开都停在第一项「无限」。

**问题**：当前 `MaxSelectCount = 5` 时，用户点「最多可选」看到的是「无限」被选中，容易误改，且看不出当前配置。

**建议**：利用 `optionalNumberColumns` 的索引恰好等于数值这一特性，为 picker 增加回显。推荐把 `columns` 改成「对象数组」显式分离显示文本与数值，同时解决回显与取值脆弱问题：

```ts
data: {
  // { text, value } 形式，value 为数量（0 表示无限）
  optionalNumberColumns: [{ text: '无限', value: 0 }] as { text: string; value: number }[]
}

// observer 里
'formData.Matrix.Options': function (options) {
  this.setData({
    optionalNumberColumns: [
      { text: '无限', value: 0 },
      ...options.map((_, i) => ({ text: String(i + 1), value: i + 1 }))
    ]
  })
}

onOptionalNumberPopupConfirm(event) {
  const selected = event.detail.value // { text, value }
  const { _optionalActionType } = this.data
  const key = _optionalActionType === 'maximum'
    ? 'formData.Matrix.MaxSelectCount'
    : 'formData.Matrix.MinSelectCount'
  const result = _optionalActionType === 'maximum'
    ? { maximumSelectableText: selected.text }
    : { minimumSelectableText: selected.text }

  this.setData({ showNumberPopup: false, ...result, [key]: selected.value })
}
```

并在 `van-picker` 上加 `default-index`（打开前按当前值算好 index 存到一个数据字段），或改用 `value-key="value"` 绑定。这样不再依赖「数组索引 == 数值」的隐式约定。

---

### 🟠 6. 列表 `wx:key` 应用稳定编号 `Value`

**现状**（`index.wxml` 第 10、30、62、66、68 行）全部用 `wx:key="index"`。

**问题**：需求反复强调每一项有「稳定编号 `Value`」，它正是为稳定标识服务的。用 `index` 作为 key，在删除中间项时会让后续节点被复用，虽当前 `value="{{item.Text}}"` 是受控绑定能兜底，但这是隐性隐患，且与 PC 端 `:key="item.Value"` 的做法不一致。

**建议**：全部改为 `wx:key="Value"`：

```html
<view wx:for="{{formData.Matrix.Titles}}" wx:key="Value">
  <view wx:for="{{formData.Matrix.Options}}" wx:key="Value">
```

> 注意：`wx:key="Value"` 要求数组项存在 `Value` 字段，当前数据结构满足。改 key 后 `data-index="{{index}}"` 仍可继续用索引，删除逻辑无需改动；但更健壮的是删除时传 `data-value="{{item.Value}}"` 并按 `Value` 过滤（见下一条）。

---

### 🟡 7. 删除操作建议按 `Value` 过滤

**现状**（`index.ts:147-164`）：按数组下标 `index` 过滤。

```ts
Titles.filter((_, idx) => idx !== index)
```

**问题**：能工作，但依赖 `data-index` 与真实下标一致。若配合 `wx:key="Value"` 改造，建议直接传稳定编号，语义更清晰、更抗错：

```ts
onRemoveAction(event) {
  const { type, value } = event.currentTarget.dataset // data-value="{{item.Value}}"
  const { Titles, Options } = this.data.formData.Matrix
  const result = type === 'title'
    ? Titles.filter(item => item.Value !== Number(value))
    : Options.filter(item => item.Value !== Number(value))
  // ...
}
```

（`dataset` 里的值统一转 `Number`，避免字符串与数字 `!==` 的隐性坑。）

---

### 🟡 8. 页面构造方式与项目不一致

**现状**：本页用 `Component({})`（`index.ts:10`），项目其余页面（`join-grade`、`role-select`、`home`、`manage`）均为 `Page({})`。

**问题**：`Component()` 构造页面是官方支持的写法（此时页面生命周期需写进 `methods`，`onLoad` 放在 `methods` 里确实能被调用），但本页并没有用到必须用 Component 才有的特性，反而：

- 与全项目风格不一致，增加心智负担；
- `lifetimes.attached` 是空函数，暴露了作者对「组件生命周期 vs 页面生命周期」的混淆；
- 唯一的动机可能是想用 `observers` 监听 `Options` 重算可选数量列，但这项逻辑很轻，直接在增删处手动重算即可，不必为此引入 Component。

**建议**：改为 `Page({})`，把 `methods` 里的方法提升到顶层，把 `observers` 换成在 `addOptionAction` / `onRemoveAction` / `onConfirmDialog`（选项分支）里显式调用 `this.refreshOptionalNumberColumns()`。

```ts
Page({
  data: { /* ... */ },
  onLoad() { /* ... */ },

  refreshOptionalNumberColumns() {
    const length = this.data.formData.Matrix.Options.length
    this.setData({
      optionalNumberColumns: [
        '无限',
        ...Array.from({ length }, (_, i) => i + 1)
      ]
    })
  }
})
```

若坚持保留 `Component()` 也无功能错误，但应删除空的 `lifetimes.attached`。

---

### 🟡 9. 其他小问题

- **`onSubmit` 防重入**：`button` 虽 `disabled="{{submitting}}"`，但 `setData` 异步，快速连点可能触发两次。建议开头加 `if (this.data.submitting) return`（参考 `role-select` 的写法）。

- **模板加载无 loading 状态**：需求要求「模板加载」状态。`onLoad` 里 `await get` 前建议置一个 `loading: true` 并用骨架/加载提示，`finally` 复位。

- **保存成功文本未回填同步**：`onLoad` 只 `setData({ formData })`，若模板返回非 0 的 `MinSelectCount`/`MaxSelectCount`，`minimumSelectableText`/`maximumSelectableText` 仍是初始「无限」，会显示错。回填时按数值同步这两个文本。

- **`.van-field-body` 全局样式**（`index.scss:118-120`）：小程序页面样式默认隔离，无法穿透 `van-field` 自定义组件内部，这段实际不生效，建议删除；需要调整组件内部样式应使用 `custom-style` 或 `input-class` 等组件暴露的 class 通道。

- **footer 死占位**（`index.wxml:77-80`）：「矩阵多选 + arrow-down」没有交互意义，若非设计需要可移除。

- **`van-radio` 预览**（`index.wxml:69`）：`name=""` 让所有 radio 同组互斥，纯展示场景建议给 `disabled` 或按行列生成唯一 `name`，避免误以为可交互。

- **`van-button` 未使用**（`index.json:7`）：`usingComponents` 里声明了 `van-button` 但页面没用到，可移除。

---

## 三、关联类型文件（附带提示）

`apps/miniprogram/src/types/field/index.ts:29-34`：

```ts
export type ResponseData = {
  Id: number
  Status: 'saved'
  Message: string
  Field: Matrix          // ← 类型不准确
}
```

Mock 实际返回的 `Field` 是完整字段（`FieldType / Name / Tip / IsRequired / IsHidden / Matrix`），即 `DataField`，而不是 `Matrix`。虽然小程序端目前未使用 `response.Data.Field`，但类型应改为：

```ts
import type { DataField } from './index' // 或同文件直接引用

export type ResponseData = {
  Id: number
  Status: 'saved'
  Message: string
  Field: DataField
}
```

---

## 四、修改优先级清单（建议按序处理）

1. 保存成功反馈 + 删除 `console.log`/`TODO`（高）
2. 批量编辑空行过滤 `trim()`（高）
3. 批量编辑后 `clampSelectCount`，并补 Min>Max 修正（高）
4. 行标题/选项上限校验（中）
5. 数量选择器回显 + 用对象数组 columns 消除索引耦合（中）
6. `wx:key="Value"` + 删除按 `Value` 过滤（中）
7. `Component` → `Page` 统一风格（低，可选）
8. 其余小问题（低）

完成 1–6 后即可满足需求全部验收标准。
