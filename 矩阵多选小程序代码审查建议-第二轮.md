# 矩阵多选小程序代码审查（第二轮：修改后校验 + 优化建议）

- 审查范围：`apps/miniprogram/src/field/pages/create-matrix-checkbox/` + `apps/miniprogram/src/types/field/index.ts`
- 视角：资深前端 / 大厂前端开发规范

---

## 一、已修复项确认 ✅

| 项 | 状态 |
| --- | --- |
| 批量编辑空行过滤改为 `item.trim() !== ''` | ✅ `index.ts:254` |
| 添加行标题/选项上限（20 / 10） | ✅ `index.ts:121、141` |
| `verification` 补上限校验 | ✅ `index.ts:386-387` |
| 批量编辑后调用 `clampSelectCount` | ✅ `index.ts:273-275` |
| 保存成功提示 | ✅ `index.ts:354`（见下方小瑕疵） |
| 删除 `console.log` / `eslint-disable no-console` | ✅ 文件头已干净 |
| `optionalNumberColumns` 改为对象数组 `{ text, value }` | ✅ `index.ts:46、66-78` |

---

## 二、仍存在的问题（按「无用逻辑 → 逻辑瑕疵 → 可优化」排序）

### 1. 无用逻辑 / 死代码

**① 空的 `lifetimes.attached`（`index.ts:61-65`）**

```ts
lifetimes: {
  attached: function () {
    // 在组件实例进入页面节点树时执行
  }
}
```

这是空实现。你保留 `Component()` 是为了用 `observers`（这个理由成立，见下文），但空 `attached` 没有任何价值，直接删掉 `lifetimes` 整个字段即可。

**② `OptionalNumberColumns` 类型已成为死代码（`types/field/index.ts:32`）**

```ts
export type OptionalNumberColumns = ['无限', ...number[]]
```

改成对象数组后，代码里已经不再 import 它（`index.ts:5` 只引了 `OptionalColumn`），删除这个类型，避免误导后续维护者。

**③ `ResponseData.Field` 类型仍错误（`types/field/index.ts:38`）**

```ts
export type ResponseData = {
  Id: number
  Status: 'saved'
  Message: string
  Field: Matrix   // ← 应为 DataField
}
```

Mock 实际返回的 `Field` 是完整字段（含 `FieldType / Name / Tip / IsRequired / IsHidden / Matrix`），即 `DataField`。虽然你目前只用了 `Message`，但类型一错，后续想用 `response.Data.Field` 回显时会被骗。改为：

```ts
export type ResponseData = {
  Id: number
  Status: 'saved'
  Message: string
  Field: DataField
}
```

**④ `optionalNumberColumns` 初始值与初始 Options 数量不一致（`index.ts:46`）**

```ts
optionalNumberColumns: [{ text: '无限', value: 0 }] as OptionalColumn[],
```

而 `data.formData.Matrix.Options` 初始有 **2 项**。微信 `observers` **不会**对 `data` 里的初始值触发，所以首屏（`onLoad` 拉取完成前）picker 里只有「无限」一项。虽然窗口期极短，但更严谨的做法是让初始列与初始 Options 对齐：

```ts
optionalNumberColumns: [
  { text: '无限', value: 0 },
  { text: '1', value: 1 },
  { text: '2', value: 2 }
] as OptionalColumn[],
```

---

### 2. 逻辑小瑕疵

**⑤ 保存成功 `icon` 用了 `none`（`index.ts:354`）**

```ts
wx.showToast({ title: response.Data.Message, icon: 'none' })
```

成功反馈应带成功图标，`icon: 'none'` 会显示一个无图标 toast，视觉上像错误提示：

```ts
wx.showToast({ title: response.Data.Message, icon: 'success' })
```

**⑥ `onSubmit` 仍缺防重入（`index.ts:335`）**

`button` 的 `disabled="{{submitting}}"` 生效有 `setData` 异步窗口，快速连点可能双提交。参考 `role-select` 的写法，入口加一句：

```ts
async onSubmit() {
  if (this.data.submitting) return
  // ...
}
```

**⑦ `onLoad` 仍无「模板加载中」状态（`index.ts:81-95`）**

需求第 10 条包含「模板加载」状态。建议加一个 `loading` 字段 + 顶部加载提示（或骨架屏），`finally` 复位。

**⑧ 数量选择器仍未回显当前值（`index.wxml:89`）**

picker 仍是无 `default-index` 的裸绑定，每次打开都停在「无限」。即便 columns 改成对象数组，回显问题没有解决。方案见下文「优化 4」。

**⑨ 列表 `wx:key` 仍是 `index`（`index.wxml` 第 10、30、62、66、68 行）**

改为 `wx:key="Value"`（需求强调的稳定编号），与 PC 端 `:key="item.Value"` 对齐，避免删除中间项时的节点复用隐患。

---

## 三、回答你的问题：`clampSelectCount` 那段要不要改

你选中的这段：

```ts
if (MaxSelectCount > optionCount) {
  matrixPatch.MaxSelectCount = optionCount
  displayPatch.maximumSelectableText = !optionCount ? '无限' : String(optionCount)
}
if (MinSelectCount > optionCount) {
  matrixPatch.MinSelectCount = optionCount
  displayPatch.minimumSelectableText = !optionCount ? '无限' : String(optionCount)
}
```

**结论：逻辑本身是对的、不用改；但它背后的设计值得重构——它是在为「冗余状态」打补丁。**

核心问题：`minimumSelectableText` / `maximumSelectableText` 是 `MinSelectCount` / `MaxSelectCount` 的**派生值**，本不该各存一份。现在的代价是这三处都要手工同步文本：

1. `clampSelectCount`（删除/批量编辑后）
2. `onOptionalNumberPopupConfirm`（选择后 `...result` 里带 text）
3. `data` 初始值（硬编码「无限」）

一处漏同步就显示错。你已经用 `Component` + `observers` 了，正好用 observers 把「数值 → 文本」变成自动派生，让 `displayPatch` 和那两个三元表达式整体消失，实现**单一数据源（SSOT）**。

---

## 四、大厂规范下的最优解

### 优化 1：用 observers 派生显示文本，删除冗余状态（最重要）

```ts
observers: {
  'formData.Matrix.Options': function (newOptions: SurveyItem[]) {
    this.setData({
      optionalNumberColumns: [
        { text: '无限', value: 0 },
        ...newOptions.map((_, i) => ({ text: String(i + 1), value: i + 1 }))
      ]
    })
  },

  // 显示文本从数值派生，不再手工同步
  'formData.Matrix.MinSelectCount': function (count: number) {
    this.setData({ minimumSelectableText: count === 0 ? '无限' : String(count) })
  },
  'formData.Matrix.MaxSelectCount': function (count: number) {
    this.setData({ maximumSelectableText: count === 0 ? '无限' : String(count) })
  }
}
```

于是 `clampSelectCount` 只维护数值，彻底简化：

```ts
clampSelectCount() {
  const { MinSelectCount, MaxSelectCount, Options } = this.data.formData.Matrix
  const optionCount = Options.length

  const patch: Partial<Matrix> = {}
  if (MaxSelectCount > optionCount) patch.MaxSelectCount = optionCount
  if (MinSelectCount > optionCount) patch.MinSelectCount = optionCount

  // 交叉兜底：两者都超过时，先取选项数，再保证 Min ≤ Max
  if (patch.MinSelectCount !== undefined && patch.MaxSelectCount !== undefined) {
    patch.MinSelectCount = Math.min(patch.MinSelectCount!, patch.MaxSelectCount!)
  }

  if (Object.keys(patch).length === 0) return

  this.setData({
    formData: { ...this.data.formData, Matrix: { ...this.data.formData.Matrix, ...patch } }
  })
}
```

`onOptionalNumberPopupConfirm` 也只需写数值，文本交给 observer：

```ts
onOptionalNumberPopupConfirm(event: WechatMiniprogram.CustomEvent<{ value: OptionalColumn; index: number }>) {
  const { value } = event.detail.value
  const { _optionalActionType } = this.data
  const key = _optionalActionType === 'maximum'
    ? 'formData.Matrix.MaxSelectCount'
    : 'formData.Matrix.MinSelectCount'

  this.setData({ showNumberPopup: false, [key]: value })
}
```

> 说明：observer 监听的是「值路径」，`setData({ 'formData.Matrix.MaxSelectCount': v })` 与整体 `setData({ formData })` 都会触发它；observer 里只 setData 文本字段，不会回环。这是小程序里替代计算属性（computed）的惯用做法。

### 优化 2：抽象 setData 样板，消除重复展开

现在每个方法都在手写三层展开。抽两个 helper，可读性大幅提升：

```ts
methods: {
  _updateField(patch: Partial<DataField>) {
    this.setData({ formData: { ...this.data.formData, ...patch } })
  },
  _updateMatrix(patch: Partial<Matrix>) {
    this.setData({
      formData: { ...this.data.formData, Matrix: { ...this.data.formData.Matrix, ...patch } }
    })
  },
  // ...
}
```

改造后：

```ts
onTitleChange(event) {
  this._updateField({ Name: event.detail })
},
addTitleAction() {
  const { Titles, TitleMaxIndex } = this.data.formData.Matrix
  if (Titles.length >= MAX_TITLE_COUNT) { wx.showToast(...); return }
  this._updateMatrix({ Titles: [...Titles, { Text: '', Value: ++TitleMaxIndex }], TitleMaxIndex })
},
onRequiredSwitchChange(event) {
  this._updateField({ IsRequired: event.detail })
}
```

注意：`onFieldChange` 用的 `setData({ 'formData.Matrix.Titles[0].Text': v })` 这种**路径写法其实更优**（只更新叶子、不用整体展开），保留它是对的，不要为了统一而改成整体展开。

### 优化 3：魔法数字提常量

与 Mock 侧的 `MIN_TITLE_COUNT / MAX_TITLE_COUNT / MIN_OPTION_COUNT / MAX_OPTION_COUNT` 对齐：

```ts
const FIELD_TYPE = 18
const MAX_TITLE_COUNT = 20
const MAX_OPTION_COUNT = 10
const MIN_TITLE_COUNT = 2
const MIN_OPTION_COUNT = 2
const MAX_NAME_LENGTH = 100
```

`addTitleAction`、`verification` 里直接用，避免散落的 20 / 10 / 100。

### 优化 4：picker 回显当前值

利用「columns 索引 == value」的特性，打开时算好索引并强制重建 picker：

`index.wxml`：

```html
<van-popup show="{{showNumberPopup}}" position="bottom" bind:close="onOptionalNumberPopupCancel">
  <van-picker
    wx:if="{{showNumberPopup}}"
    columns="{{optionalNumberColumns}}"
    default-index="{{activePickerIndex}}"
    bind:confirm="onOptionalNumberPopupConfirm"
    bind:cancel="onOptionalNumberPopupCancel"
  />
</van-popup>
```

`index.ts`：

```ts
data: {
  activePickerIndex: 0,   // 注意：不能用 _ 前缀，否则 pureDataPattern 会拦截不参与渲染
  // ...
},

onShowOptionalNumberPopup(event) {
  const actionType = event.currentTarget.dataset.actionType
  const { MinSelectCount, MaxSelectCount } = this.data.formData.Matrix
  this.setData({
    showNumberPopup: true,
    _optionalActionType: actionType,
    activePickerIndex: actionType === 'maximum' ? MaxSelectCount : MinSelectCount
  })
}
```

关键点：
- 用 `wx:if="{{showNumberPopup}}"` 让 picker 每次打开重新渲染，`default-index` 才每次都生效（否则只在首次生效）。
- 回显字段不能用 `_` 前缀，`json` 里 `pureDataPattern: "^_"` 会把 `_` 开头的字段当纯数据、不发送到视图层。

### 优化 5：`verification` 去重 `Name.trim()`

```ts
verification(): string | null {
  const { Name, Matrix: { Titles, Options, MinSelectCount, MaxSelectCount } } = this.data.formData
  const name = Name.trim()

  if (!name) return '标题不能为空'
  if (name.length > 100) return '标题最多 100 字'
  // ...
}
```

---

## 五、最终处理清单（建议顺序）

| 优先级 | 动作 |
| --- | --- |
| 立即 | 删 `lifetimes` 空实现、删 `OptionalNumberColumns` 类型、修 `ResponseData.Field` 类型 |
| 立即 | 保存成功 `icon: 'success'`、`onSubmit` 加防重入 |
| 推荐 | 优化 1（observers 派生文本，重构 `clampSelectCount`，删除 `displayPatch`）|
| 推荐 | 优化 2（`_updateField/_updateMatrix` 去重）、优化 3（常量提取）|
| 推荐 | 优化 4（picker 回显）、`wx:key="Value"`、`onLoad` loading |
| 可选 | `optionalNumberColumns` 初始值与初始 Options 对齐 |

其中「优化 1」直接回答了你关于 `clampSelectCount` 那段要不要改的问题：**逻辑不用动，但推荐借 observers 让那段连同 `displayPatch` 一起消失，从根本上消除「文本与数值双份状态」的同步成本。**
