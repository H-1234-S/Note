# 同步组件和异步组件

> 同步组件指的是 render 阶段，组件执行时不会暂停，也就是 await 等待数据返回的组件，而是直接返回 JSX

> 异步组件指的是组件被 async 标记并且进行异步操作的组件，可以 await 等待数据，之后再返回 JSX


异步组件执行流程：
```
在 render 阶段，执行异步组件，得到的是 pending 状态的 Promise

需要
```

# 首屏加载慢怎么优化

# 渲染时卡顿怎么排查