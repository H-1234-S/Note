``` mermaid
graph LR
    subgraph Client_Side [前端 React + Zustand]
        A[Chat UI 输入框] -->|1. 提交消息| B{handleSend 函数}
        B -->|2. 检查图片| C[upload.js 工具]
        F[onSnapshot 监听器] -->|7. 实时更新状态| G[useChatStore / UI 渲染]
    end

    subgraph Firebase_Services [后端 Firebase 服务]
        C -->|3. 上传字节流| D[Firebase Storage]
        D -->|4. 返回 URL| B
        B -->|5. 写入消息数组| E[(Firestore: chats)]
        B -->|6. 更新预览索引| H[(Firestore: userchats)]
        E -.->|实时推送数据| F
    end

    style A fill:#f9f,stroke:#333,stroke-width:2px
    style G fill:#bbf,stroke:#333,stroke-width:2px
    style E fill:#dfd,stroke:#333,stroke-width:2px
    style H fill:#dfd,stroke:#333,stroke-width:2px
```
