# React 为什么要将状态视为不可变

> React 通过判断引用是否发生变化来判断状态是否更新

`React` 使用 `Object.is` 来判断状态是否发生变化，也就是使用浅比较

> React 为什么不用深比较

React 为了性能其实使用的是浅比较，时间复杂度为 O(1)；如果每次都深比较那么sh

> 不可变数据使更新更容易推导

> 有利于 React 的性能优化

例如使用 `memo`，`memo` 也是进行浅比较