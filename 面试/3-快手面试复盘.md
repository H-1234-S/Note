# 项目拷打

# 宏任务、微任务、raf

> raf 在浏览器下一帧绘制前执行

```js
console.log(1)
setTimeout(() => console.log(2),0)
requestAnimationFrame(() => console.log(3))
Promise.resolve.then(() => console.log(4))
console.log(5)
```

`requestAnimationFrame` 不是 `Event Loop` 的固定阶段，执行时机取决于浏览器的下一帧何时渲染

`requestAnimationFrame` 和 `setTimeout` 执行的先后顺序取决于浏览器下一次何时渲染

> 举个例子：如果 JS 一直在执行，浏览器根本没有时间渲染，因此 requestAnimationFrame 就一直不会执行
# 浏览器每一帧都在做什么

浏览器每 `16.67` 毫秒进行一帧的更新，但是浏览器每一帧做的事情是不固定的

理想情况下，一次渲染机会大致会经历：
```
执行一个宏任务（Task）
        ↓
清空所有微任务（Microtask）
        ↓
浏览器进入一次渲染机会
        ↓
执行所有 requestAnimationFrame 回调
        ↓
如有需要：
  Style（样式计算）
  Layout（重排）
  Paint（重绘）
  Composite（图层合成）
        ↓
提交给 GPU，等待显示器显示
```

但是如果 JS 执行时间过长，浏览器就没办法进行每一帧的更新，因为主线程中JS执行和渲染是互斥的
# useEffect、useLayoutEffect

# Promise.all、Promise.allSettled

# async、await

# 手写并发控制器
