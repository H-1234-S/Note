# 布局

# 定位

# 盒模型

# 块、行、行内块

块级元素，例如`div`， 独占一行，可以独立设置宽高

行内元素，例如`span`，一行可以显示多个元素，不可以设置宽高，上下边距无效

行内块元素，一行可以显示多个元素，但是可以设置宽高

# 判断数据类型


# JS 存储

## cookie

设计目的是为了解决如何在多次请求之间携带状态

大小通常是 4kb，每次随请求发送，携带在 headers 中
## LocalStorage

本地持久化存储数据，永久有效，除非手动清除，大小通常是 5mb

只能存储字符串，对于对象可以用 `JSON.stringify()` 转为字符串存储

``` js
localStorage.setItem("myCat", "Tom")

localStorage.getItem("myCat")

localStorage.removeItem("myCat")

// 移除所有
localStorage.clear();
```
## SessionStorage

SessionStorage 与 LocalStorage 类似，但是 SessionStorage 是会话状态，意思是当关闭标签页面数据就会被清空

``` js
// 保存数据到 sessionStorage
sessionStorage.setItem("key", "value");

// 从 sessionStorage 获取数据
let data = sessionStorage.getItem("key");

// 从 sessionStorage 删除保存的数据
sessionStorage.removeItem("key");

// 从 sessionStorage 删除所有保存的数据
sessionStorage.clear();
```
## IndexedDB

IndexedDB 可以看作为运行在浏览器中的**非关系型数据库**，用于在客户端存储大量的结构化数据（也包括文件/二进制大型对象（blob））

存储空间一般没有限制，永久存储，除非手动删除

# 前端怎么解决跨域

跨域是因为浏览器的同源策略，才会有跨域这个问题，前端一般不能解决，但是可以绕过

跨域 CORS 正常来说是后端配置响应头，浏览器验证后运行 JS 读取响应

## 怎么绕过？

在开发阶段，在本地搭一个代理服务器，将请求转发；因为跨域是浏览器和服务器之间的问题，那么将请求从服务端发到服务端就不存在跨域；但是只在开发环境下有效，打包上线本地node服务就没有了


